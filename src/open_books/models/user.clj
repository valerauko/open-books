(ns open-books.models.user)

; TODO
; * authenticate user with mail / pass
; * authenticate user with session key
; * fetch data for current user
; * fetch public data for other user
; * update user data
; * destroy user
;
; * list / add / remove follows
